{
  "name": "typesafe-pro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @typesafe-pro/web",
    "dev:api": "uv run --project apps/api --env-file .env uvicorn proxy.main:app --app-dir apps/api --port 8010 --reload --no-access-log",
    "check": "uv run --project apps/api ruff check apps/api deploy && uv run --project apps/api ruff format --check apps/api deploy",
    "test": "uv run --project apps/api pytest apps/api/tests",
    "cf:pages:create": "bash scripts/cf-env.sh wrangler pages project create typesafe-pro --production-branch=main",
    "cf:pages:ensure": "bash scripts/ensure-pages-project.sh typesafe-pro",
    "deploy:web": "npm run deploy --workspace @typesafe-pro/web"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
