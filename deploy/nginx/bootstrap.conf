server {
    listen 80;
    listen [::]:80;
    server_name api.typesafe.pro;
    access_log off;
    error_log /dev/null;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }
    location / {
        return 503;
    }
}
